doctype html 
html 
    head
        title Iniciar Sesión 
        meta(name='viewport', content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet', href='/css/styles.css')
    body
      .area 
        h2 Iniciar Sesión 
        form.formimg(action='/login' method='POST' class='login-form')
          label(for='username' class='form-label' id='labelform') Usuario
          input(type='text' name='username' id='username' placeholder='Usuario' class='form-input', required)

          label(for='password' class='form-label' id='labelform') Contraseña
          input(type='password' name='password' id='password' placeholder='Contraseña' class='form-input', required)

          button(type='submit') Iniciar Sesión

        p ¿No tienes una cuenta? 
          a(href='/register') Regístrate